# BEGIN TERRAFORM
%{ for master in masters ~}
${ master.tags.Name }  ${ master.private_ip }
%{ endfor ~}
%{ for worker in workers ~}
${ worker.tags.Name } ${ worker.private_ip }
%{ endfor ~}
# END TERRAFORM
